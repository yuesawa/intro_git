# xTBパッケージの計算制限と推奨事項

## 概要

このドキュメントでは、xTB（GFN2-xTB）を使用したHOMO/LUMO計算における技術的な制限と実用上の推奨事項をまとめています。

---

## 1. 原子数の制限

### ハードコード制限（重要）

**⚠️ 255原子の制限**

xTBのデフォルトビルドには**255原子**のハードコード制限があります：

- ✅ **255原子以下**: 正常に動作
- ❌ **256原子以上**: セグメンテーションフォールト（クラッシュ）

**影響を受けるシステム例**:
- 大型タンパク質フラグメント
- フラーレンC240以上
- 大型金属クラスター
- 長鎖ポリマー

**回避策**:
```bash
# xTBをソースからコンパイルして制限を変更可能
# ただし、メモリ要件が大幅に増加する点に注意
```

### 実用的な原子数の範囲

| 分子サイズ | 原子数 | 計算時間（目安） | 用途例 |
|-----------|--------|----------------|--------|
| 極小分子 | 1-10 | < 1秒 | H₂O, CH₄, NH₃ |
| 小分子 | 10-50 | 1-5秒 | ベンゼン、医薬品分子 |
| 中分子 | 50-100 | 5-20秒 | ペプチド、天然物 |
| 大分子 | 100-200 | 20-60秒 | タンパク質フラグメント |
| 超大分子 | 200-255 | 1-3分 | 大型生体分子（制限近く） |

**推奨**: HOMO/LUMO計算では **200原子以下** を推奨

---

## 2. 元素の対応範囲

### 対応元素

**GFN2-xTB対応範囲: Z = 1-86（水素～ラドン）**

#### 完全対応
- **主族元素**: H, C, N, O, F, S, P, Cl, Br, I など（すべて）
- **遷移金属**: Sc-Zn, Y-Cd, La-Hg（すべて）
- **ランタノイド**: Ce-Lu（全元素、一部は線形補間）

#### 元素リスト（周期表順）

```
Period 1: H, He
Period 2: Li, Be, B, C, N, O, F, Ne
Period 3: Na, Mg, Al, Si, P, S, Cl, Ar
Period 4: K, Ca, Sc, Ti, V, Cr, Mn, Fe, Co, Ni, Cu, Zn, Ga, Ge, As, Se, Br, Kr
Period 5: Rb, Sr, Y, Zr, Nb, Mo, Tc, Ru, Rh, Pd, Ag, Cd, In, Sn, Sb, Te, I, Xe
Period 6: Cs, Ba, La-Lu (lanthanides), Hf, Ta, W, Re, Os, Ir, Pt, Au, Hg, Tl, Pb, Bi, Po, At, Rn
```

#### 非対応元素
- **アクチノイド**: Ac, Th, U, Pu など（Z > 86）
- **超重元素**: すべて非対応

**注意**: 対応元素であっても、特殊な酸化状態や配位環境では精度が低下する場合があります。

---

## 3. メモリ要件

### 基本的なメモリスケーリング

xTBは密行列代数を使用するため、メモリ要件は原子数の2乗に近い形でスケールします。

#### 目安（シングルスレッド）

| 原子数 | 推定メモリ | 例 |
|-------|----------|-----|
| 10 | < 10 MB | ベンゼン |
| 50 | 50-100 MB | 中分子医薬品 |
| 100 | 200-400 MB | ペプチド |
| 200 | 800 MB - 1.5 GB | 大型分子 |
| 255 | 1.5-2 GB | 制限近く |
| 500* | 5-8 GB | *制限超過（要カスタムビルド） |
| 1000* | 16-20 GB | *制限超過（要カスタムビルド） |
| 3000* | 50-60 GB | *制限超過（要カスタムビルド） |

*255原子を超える計算にはカスタムビルドが必要

#### マルチスレッド使用時

```
総メモリ ≈ ベースメモリ × スレッド数
```

**例**: 100原子の分子を8スレッドで計算
```
必要メモリ ≈ 400 MB × 8 = 3.2 GB
```

### 推奨メモリ設定

```bash
# 環境変数での設定
export OMP_STACKSIZE=4G    # スタックサイズ
ulimit -s unlimited        # システムスタック制限を解除

# 並列計算時のスレッド数制限
export OMP_NUM_THREADS=4   # 使用スレッド数
```

---

## 4. 電荷とスピン状態

### 電荷

- **対応範囲**: 任意の整数電荷（-10 ～ +10程度を推奨）
- **注意**: 極端な電荷（±5以上）では計算が不安定になる可能性

```bash
# xTBコマンドでの電荷指定
xtb molecule.xyz --chrg 1    # +1電荷
xtb molecule.xyz --chrg -2   # -2電荷
```

### スピン多重度

- **一重項**: デフォルト（閉殻系）
- **多重項**: `--uhf` オプションで開殻系に対応

```bash
# ラジカル（二重項）の計算
xtb molecule.xyz --uhf 1     # 不対電子1個

# 三重項の計算
xtb molecule.xyz --uhf 2     # 不対電子2個
```

**注意**: HOMO/LUMO計算では、開殻系の場合αスピンとβスピンで異なる値が得られます。

---

## 5. 分子の性質による制限

### 対応可能な系

✅ **推奨**:
- 有機分子（C, H, N, O, S, P, ハロゲン中心）
- 金属錯体（d-block金属含む）
- 生体分子（タンパク質、核酸のフラグメント）
- 非共有結合複合体
- 溶液中の分子（溶媒和モデルと併用）

⚠️ **注意が必要**:
- 高スピン金属錯体
- f-block元素を含む系（ランタノイド）
- 極端に歪んだ構造
- 非常に強い相関電子系

❌ **非推奨**:
- アクチノイド含有系（パラメータなし）
- 固体物理系（※周期境界条件は一部対応）
- 励起状態計算（GFN2-xTBは基底状態専用）

---

## 6. 計算精度の限界

### HOMO/LUMOエネルギーの精度

**vs. DFT (B3LYP/def2-TZVP)との比較**:
- **絶対値誤差**: ±0.5 eV程度
- **相対的な傾向**: 良好（分子間の比較に有用）
- **用途**: スクリーニング、トレンド解析

### 精度が低下するケース

1. **強い電子相関系**
   - 遷移金属の多核錯体
   - ビラジカル系

2. **非常に大きなHOMO-LUMOギャップ**
   - 希ガス原子
   - 非常に安定なイオン結晶

3. **極端な電荷分布**
   - 強い分極が生じる系
   - 高電荷イオン

**推奨**: 重要な結果はDFTや高レベル計算で検証すること

---

## 7. 計算時間の制限

### タイムアウト設定

大規模システムでは計算が収束しない場合があるため、タイムアウトを設定することを推奨：

```python
# 現在のスクリプトでのタイムアウト設定
subprocess.run(
    cmd,
    timeout=300  # 5分でタイムアウト
)
```

### 推奨タイムアウト時間

| 原子数 | 推奨タイムアウト |
|-------|--------------|
| < 50 | 60秒 |
| 50-100 | 180秒（3分） |
| 100-200 | 300秒（5分） |
| 200-255 | 600秒（10分） |

---

## 8. ファイルサイズ制限

### 入力ファイル

- **XYZ形式**: 制限なし（実質的には255原子まで）
- **SDF形式**: 分子あたり数MB程度を推奨

### 出力ファイル

- **標準出力**: 分子あたり10-100 KB程度
- **詳細出力**: 分子あたり1-10 MB

### ディスク容量の推定

**10万分子の計算**:
```
入力SDF: 100,000分子 × 10 KB/分子 = 1 GB
出力CSV: 100,000行 × 100 bytes/行 = 10 MB
一時ファイル: 100,000分子 × 50 KB/分子 = 5 GB（処理中）

合計推定: 約 6-7 GB
```

---

## 9. スクリプトへの検証機能の追加

### 推奨される入力検証

```python
def validate_molecule(mol):
    """
    分子がxTB計算に適しているか検証

    Returns:
        tuple: (is_valid, error_message)
    """
    # 原子数チェック
    num_atoms = mol.GetNumAtoms()
    if num_atoms > 255:
        return False, f"Too many atoms: {num_atoms} (max: 255)"

    if num_atoms == 0:
        return False, "No atoms in molecule"

    # 元素チェック
    supported_elements = set(range(1, 87))  # Z = 1-86
    for atom in mol.GetAtoms():
        atomic_num = atom.GetAtomicNum()
        if atomic_num not in supported_elements:
            symbol = atom.GetSymbol()
            return False, f"Unsupported element: {symbol} (Z={atomic_num})"

    return True, "OK"
```

---

## 10. エラー対処方法

### よくあるエラーと対処法

#### セグメンテーションフォールト
```
Cause: 原子数が255を超えている
Solution: 分子を分割するか、カスタムビルドを使用
```

#### メモリ不足
```
Cause: 利用可能メモリ不足
Solution:
  - スレッド数を減らす
  - 小さい分子から順に処理
  - スワップ領域を増やす
```

#### 収束しない
```
Cause: 不安定な電子構造
Solution:
  - 初期構造を最適化
  - 電荷/スピン状態を見直す
  - より小さい分子で検証
```

#### 対応していない元素
```
Cause: Z > 86の元素を含む
Solution: その分子をスキップするか、DFT計算を使用
```

---

## 11. まとめ

### ✅ 実用的な推奨範囲

| 項目 | 推奨範囲 |
|------|---------|
| 原子数 | 10-200原子 |
| 元素 | H, C, N, O, S, P, ハロゲン、遷移金属（Z ≤ 86） |
| 電荷 | -3 ～ +3 |
| メモリ | 分子あたり2 GB以下 |
| 計算時間 | 分子あたり5分以内 |

### 📊 大規模計算（10万分子）の実現可能性

**小-中分子（50原子以下）**: ✅ **問題なし**
- メモリ: 数十GB程度
- 時間: 並列処理で数時間～1日

**大分子（100-200原子）**: ⚠️ **要注意**
- メモリ: 数百GB必要
- 時間: HPCクラスタ推奨

**超大分子（200原子以上）**: ❌ **推奨しない**
- 255原子の制限に近づく
- DFTやフォースフィールドの検討を推奨

---

## 参考文献

1. Bannwarth, C. et al. *J. Chem. Theory Comput.* 2019, 15, 1652-1671
   "GFN2-xTB—An Accurate and Broadly Parametrized Self-Consistent Tight-Binding Quantum Chemical Method"

2. xTB GitHub Issues:
   - Issue #83: "Segmentation fault after exceeding certain number of atoms"
   - Issue #98: "Memory Overflow of the ASE calculator"

3. xTB Documentation: https://xtb-docs.readthedocs.io/

---

**最終更新**: 2025-11-15
