# HOMO/LUMO計算の性能推定レポート

## 10万分子の計算時間推算

### 前提条件

- **計算手法**: GFN2-xTB
- **計算対象**: HOMO/LUMOエネルギーとギャップ
- **並列効率**: 85%（典型的な値）
- **オーバーヘッド**: 分子あたり0.17秒（RDKit処理、I/O、解析）

### 分子サイズ別の計算時間（1分子あたり）

| 分子サイズ | 原子数 | xTB計算時間 | 総処理時間* | 例 |
|-----------|--------|------------|------------|-----|
| 小分子 | 10-20 | 2.0秒 | 2.17秒 | メタノール、ベンゼン、ピリジン |
| 中分子 | 20-50 | 5.0秒 | 5.17秒 | ナフタレン、アントラセン、一般的な医薬品 |
| 大分子 | 50-100 | 15.0秒 | 15.17秒 | フラーレンC60、複雑な天然物 |
| 超大分子 | 100-200 | 45.0秒 | 45.17秒 | タンパク質フラグメント、大型多環芳香族 |

*総処理時間 = xTB計算時間 + オーバーヘッド

---

## シナリオ別の計算時間推算

### ケース1: 小分子（平均12原子）- 例: sample_molecules.sdf

**対象**: ベンゼン、ピリジン、メタノールなどの小分子ライブラリ

| 並列度 | 計算時間 | 実時間（時：分） | 実時間（日） |
|--------|---------|----------------|-------------|
| **1コア（逐次）** | **60.3時間** | **2日12時間** | **2.51日** |
| 4コア | 17.7時間 | 17時間44分 | 0.74日 |
| 8コア | 8.9時間 | 8時間52分 | 0.37日 |
| 16コア | 4.4時間 | 4時間26分 | 0.18日 |
| 32コア | 2.2時間 | 2時間13分 | 0.09日 |

**推奨**: 8-16コアでの並列処理（約5-9時間で完了）

---

### ケース2: 中分子（平均30原子）- 例: 創薬ライブラリ

**対象**: 一般的な医薬品分子、複雑な有機分子

| 並列度 | 計算時間 | 実時間（時：分） | 実時間（日） |
|--------|---------|----------------|-------------|
| **1コア（逐次）** | **143.6時間** | **5日23時間** | **5.98日** |
| 4コア | 42.2時間 | 1日18時間 | 1.76日 |
| 8コア | 21.1時間 | 21時間7分 | 0.88日 |
| 16コア | 10.6時間 | 10時間34分 | 0.44日 |
| 32コア | 5.3時間 | 5時間17分 | 0.22日 |

**推奨**: 16-32コアでの並列処理（約5-11時間で完了）

---

### ケース3: 大分子（平均70原子）- 例: 天然物、大型化合物

**対象**: フラーレン、複雑な天然物、ペプチド

| 並列度 | 計算時間 | 実時間（時：分） | 実時間（日） |
|--------|---------|----------------|-------------|
| **1コア（逐次）** | **421.4時間** | **17日13時間** | **17.56日** |
| 4コア | 123.9時間 | 5日4時間 | 5.16日 |
| 8コア | 62.0時間 | 2日14時間 | 2.58日 |
| 16コア | 31.0時間 | 1日7時間 | 1.29日 |
| 32コア | 15.5時間 | 15時間30分 | 0.65日 |
| 64コア | 7.7時間 | 7時間45分 | 0.32日 |

**推奨**: 32-64コアでの並列処理（約8-16時間で完了）

---

### ケース4: 超大分子（平均150原子）- 例: タンパク質フラグメント

**対象**: タンパク質の一部、大型生体分子

| 並列度 | 計算時間 | 実時間（時：分） | 実時間（日） |
|--------|---------|----------------|-------------|
| **1コア（逐次）** | **1,254.7時間** | **52日6時間** | **52.28日** |
| 8コア | 186.0時間 | 7日18時間 | 7.75日 |
| 16コア | 93.0時間 | 3日21時間 | 3.87日 |
| 32コア | 46.5時間 | 1日22時間 | 1.94日 |
| 64コア | 23.2時間 | 23時間15分 | 0.97日 |
| 128コア | 11.6時間 | 11時間38分 | 0.48日 |

**推奨**: 64-128コアでの分散計算（約12-23時間で完了）

---

## 処理時間の内訳

各分子の処理時間は以下の要素で構成されます：

```
総処理時間 = xTB計算時間 + RDKit処理 + ファイルI/O + 出力解析
```

### オーバーヘッドの詳細（分子あたり）

| 処理 | 時間 | 割合* |
|------|------|-------|
| RDKit処理（3D座標生成など） | 0.100秒 | 58.8% |
| ファイルI/O（XYZ書き込み・読み込み） | 0.050秒 | 29.4% |
| xTB出力解析 | 0.020秒 | 11.8% |
| **合計オーバーヘッド** | **0.170秒** | **100%** |

*小分子（2秒計算）の場合、オーバーヘッドは全体の約8%

---

## 性能改善の推奨事項

### 1. 並列処理の実装（最優先）

**効果**: 3-27倍の高速化

```python
# multiprocessingを使用した並列処理の例
from multiprocessing import Pool

def process_molecule_wrapper(args):
    mol, mol_name, xtb_binary = args
    return calculate_homo_lumo_xtb(mol, mol_name, xtb_binary)

# 並列処理
with Pool(processes=16) as pool:
    results = pool.map(process_molecule_wrapper, molecule_list)
```

**推奨コア数**:
- 小分子: 8-16コア
- 中分子: 16-32コア
- 大分子: 32-64コア
- 超大分子: 64-128コア（分散計算推奨）

### 2. チェックポイント機能の実装

長時間計算では中断・再開機能が必須：

```python
# 1000分子ごとに中間結果を保存
checkpoint_interval = 1000
for i, mol in enumerate(molecules):
    result = calculate_homo_lumo(mol)
    results.append(result)

    if (i + 1) % checkpoint_interval == 0:
        save_checkpoint(results, f'checkpoint_{i+1}.pkl')
```

### 3. バッチ処理の最適化

- **推奨バッチサイズ**: 1,000-10,000分子
- **メモリ管理**: 大量のSDFファイルは分割して処理
- **エラーハンドリング**: 失敗した分子をスキップして続行

### 4. 計算リソースの選択

#### 小規模（1万分子以下）
- **環境**: ワークステーション（8-16コア）
- **時間**: 数時間～1日

#### 中規模（1-10万分子）
- **環境**: HPCクラスタ（16-64コア）
- **時間**: 数時間～2日
- **ジョブスケジューラ**: Slurm, PBS推奨

#### 大規模（10万分子以上）
- **環境**: 分散計算システム
- **時間**: 並列度に依存
- **推奨**: クラウドHPC（AWS Batch, Google Cloud HPCなど）

### 5. I/O最適化

- **ローカルストレージ使用**: ネットワークストレージは避ける
- **RAMディスク活用**: 小分子の場合、tmpfsで高速化
- **バッファリング**: 結果をメモリに溜めてからディスクに書き込み

---

## 実行コマンド例

### 基本的な実行（逐次処理）

```bash
python3 calculate_homo_lumo.py molecules.sdf -o results.csv
```

### 並列処理版（実装後）

```bash
# 16コアで並列処理
python3 calculate_homo_lumo_parallel.py molecules.sdf -o results.csv --cores 16

# チェックポイント機能付き
python3 calculate_homo_lumo_parallel.py molecules.sdf -o results.csv \
    --cores 16 --checkpoint-interval 1000 --resume checkpoint_50000.pkl
```

### HPCクラスタでの実行例（Slurm）

```bash
#!/bin/bash
#SBATCH --job-name=homo_lumo
#SBATCH --nodes=1
#SBATCH --ntasks-per-node=32
#SBATCH --time=12:00:00
#SBATCH --mem=64GB

module load anaconda
module load xtb

python3 calculate_homo_lumo_parallel.py large_dataset.sdf \
    -o results.csv --cores 32
```

---

## ベンチマークツールの使用方法

時間推定ツールが付属しています：

```bash
# 基本的な推定（10万分子、平均30原子）
python3 benchmark_homo_lumo.py

# カスタム設定
python3 benchmark_homo_lumo.py -n 50000 -a 12 -c 16

# 詳細表示
python3 benchmark_homo_lumo.py --show-breakdown
```

**パラメータ**:
- `-n, --num-molecules`: 分子数
- `-a, --avg-atoms`: 平均原子数
- `-c, --cores`: CPUコア数
- `--show-breakdown`: 詳細な内訳を表示

---

## 参考文献

- xTB Documentation: https://xtb-docs.readthedocs.io/
- GFN2-xTB論文: Bannwarth, C. et al. *J. Chem. Theory Comput.* 2019, 15, 1652-1671
- 並列化のベストプラクティス: Python multiprocessing documentation

---

## まとめ

### 10万分子の計算時間（主要ケース）

| 分子サイズ | 逐次処理 | 16コア並列 | 32コア並列 | 推奨構成 |
|-----------|---------|-----------|-----------|---------|
| 小分子（12原子） | 2.5日 | 4.4時間 | 2.2時間 | 8-16コア |
| 中分子（30原子） | 6.0日 | 10.6時間 | 5.3時間 | 16-32コア |
| 大分子（70原子） | 17.6日 | 31.0時間 | 15.5時間 | 32-64コア |

**結論**: 適切な並列処理を実装することで、10万分子の計算を **数時間～1日** で完了できます。
